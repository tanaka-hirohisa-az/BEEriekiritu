# 売上予想の元になっているPRONES個人クエリ（担当者100061）

作成日: 2026/08/26

手作業Excel `売上予想8.9.10.11.12.xlsx` の「内示」「確定」は、PRONESの**個人設定クエリ**の
実行結果を貼り付けて作られています。そのクエリ定義をDBから抽出したものがこの文書です。
`売上予想.SemanticModel` の `Fact_受注見込` は、ここに書いた定義をそのまま移植しています。

## 個人クエリはDBのどこにあるか

| テーブル | 内容 |
|---|---|
| `PRONES.TB_クエリ見出情報` | クエリの見出し。`F_担当者`（社員番号）/ `F_クエリ番号` / `F_タイトル` / `F_有効区分` / `F_利用回数` / `F_更新日` |
| `PRONES.TB_クエリ明細情報` | クエリの中身。`F_クエリ番号` + `F_区分` で構成要素を表す |

`F_区分` の意味（列コメントより）:

| 区分 | 意味 | F_項目名１ | F_項目名２ | F_メモ１ | F_メモ２ |
|---|---|---|---|---|---|
| `T` | Table | テーブル名称 | テーブル別名 | — | — |
| `J` | Join | 左項目名 | 右項目名 | ジョイント種別 | — |
| `W` | Where | 項目名 | 条件値 | 評価マーク | タイプ |
| `S` | Select | 項目名 | T別名.項目名 | タイプ | 項目別名称 |
| `G` | GroupBy | 項目名 | Group | — | — |
| `O` | OrderBy | 項目名 | ソート順 | — | — |

`F_区分='T'` の `F_項目名１` が `タイトル(クエリ番号)` の形になっている行は、
**別のクエリを副問い合わせとして参照**しています。

一覧を見るクエリ:

```sql
SELECT F_クエリ番号, F_タイトル, F_有効区分, F_利用回数,
       TO_CHAR(F_更新日,'YYYY/MM/DD') AS 更新日, F_備考
FROM PRONES.TB_クエリ見出情報
WHERE TRIM(F_担当者) = '100061'
ORDER BY F_クエリ番号;
```

## 対象クエリ

| クエリ番号 | タイトル | 有効 | 利用回数 | 更新日 |
|---|---|---|---|---|
| **230220003** | 確定注文の売上予想 | ○ | 123 | **2026/08/19** |
| **220325008** | 客先別内示情報の売上予想 | ○ | 207 | 2025/08/25 |
| 161011004 | 得意先最新単価（上記2つが参照） | ○ | — | — |
| 161011003 | 得意先単価最新日（161011004が参照） | ○ | — | — |
| 200228004 | 内示最新作成日（220325008が参照） | ○ | — | — |

`230220003` の更新日が、添付Excelの最終更新日（2026/08/19）と一致します。

> 同じタイトルの旧クエリ（`200228006` / `210204003` / `220325007` など）は
> `F_有効区分='0'` で無効化され、備考に「新クエリ番号:…」と後継が書かれています。
> 現行は上表の2本です。

## 抽出した定義（等価なSQL）

### 161011003 得意先単価最新日

```sql
SELECT F_納入品目番号, F_得意先番号, MAX(F_開始日) AS F_開始日
FROM PRONES.TB_納入品目得意先単価情報
WHERE F_納入品目番号 >= '0'
GROUP BY F_納入品目番号, F_得意先番号
```

> **有効日の絞り込みが無い**点に注意。将来日付の単価が登録されていれば、それが採用されます。

### 161011004 得意先最新単価

```sql
SELECT t.F_納入品目番号, t.F_得意先番号, t.F_開始日, t.F_契約単価
FROM PRONES.TB_納入品目得意先単価情報 t
JOIN (161011003) q
  ON t.F_納入品目番号 = q.F_納入品目番号
 AND t.F_得意先番号   = q.F_得意先番号
 AND t.F_開始日       = q.F_開始日
WHERE t.F_納入品目番号 >= '0'
```

### 200228004 内示最新作成日

```sql
SELECT F_得意先, MAX(F_作成日) AS F_作成日
FROM PRONES.TB_統合計画日別情報
WHERE F_納入品目番号 > '0'
GROUP BY F_得意先
```

> **納期での絞り込みが無い**点に注意。得意先ごとの「最後に内示を受信した日」を全期間から取ります。

### 230220003 確定注文の売上予想

```sql
SELECT Tab2.F_得意先番号,
       Tab2.F_得意先略称,
       Tab6.F_得意先略称 AS 納入先略称,
       Tab1.F_納入品目番号,
       Tab1.F_在庫品目番号,
       SUM(Tab1.F_注文数)                        AS 注文数,
       MIN(Qer3.F_契約単価)                      AS 契約単価,
       SUM(Qer3.F_契約単価 * Tab1.F_注文数)      AS 金額,
       MAX(Tab4.F_回路数)                        AS 回路数,
       MAX(Tab5.F_仕入先名称)                    AS 仕入先名称
FROM      PRONES.TB_統合注文情報   Tab1
JOIN      PRONES.TB_得意先情報     Tab2 ON Tab1.F_得意先       = Tab2.F_得意先番号
JOIN      PRONES.TB_納入品目情報   Tab4 ON Tab1.F_在庫品目番号 = Tab4.F_納入品目番号
LEFT JOIN (161011004)              Qer3 ON Tab1.F_得意先       = Qer3.F_得意先番号
                                       AND Tab1.F_納入品目番号 = Qer3.F_納入品目番号
JOIN      PRONES.TB_仕入先情報     Tab5 ON Tab1.F_指示先       = Tab5.F_仕入先番号
JOIN      PRONES.TB_得意先情報     Tab6 ON Tab1.F_納入先       = Tab6.F_得意先番号
WHERE Tab1.F_納入指定日 >= :何時から
  AND Tab1.F_納入指定日 <= :何時まで
GROUP BY Tab2.F_得意先番号, Tab2.F_得意先略称, Tab6.F_得意先略称,
         Tab1.F_納入品目番号, Tab1.F_在庫品目番号
```

保存されている最後の実行値は `2023/02/01` 〜 `2023/02/31`。
月単位で `YYYY/MM/01` 〜 `YYYY/MM/31` を入れて使う運用です（文字列比較なので31日固定で問題ありません）。

### 220325008 客先別内示情報の売上予想

```sql
SELECT Tab1.F_得意先,
       Tab4.F_得意先略称,
       Tab1.F_納入品目番号,
       Tab1.F_在庫品目番号,
       SUM(Tab1.F_数量 + Tab1.F_調整数)                          AS 数量,
       MIN(Qer3.F_契約単価)                                      AS 契約単価,
       SUM(Qer3.F_契約単価 * (Tab1.F_数量 + Tab1.F_調整数))      AS 金額,
       MAX(Tab5.F_回路数)                                        AS 回路数,
       MAX(Tab6.F_仕入先名称)                                    AS 仕入先名称
FROM PRONES.TB_統合計画日別情報 Tab1
JOIN (200228004)                Qer2 ON Tab1.F_得意先       = Qer2.F_得意先
                                    AND Tab1.F_作成日       = Qer2.F_作成日
JOIN (161011004)                Qer3 ON Tab1.F_在庫品目番号 = Qer3.F_納入品目番号
                                    AND Tab1.F_得意先       = Qer3.F_得意先番号
JOIN PRONES.TB_得意先情報       Tab4 ON Tab1.F_得意先       = Tab4.F_得意先番号
JOIN PRONES.TB_納入品目情報     Tab5 ON Tab1.F_納入品目番号 = Tab5.F_納入品目番号
JOIN PRONES.TB_仕入先情報       Tab6 ON Tab1.F_指示先       = Tab6.F_仕入先番号
WHERE Tab1.F_納期 >= :何時から
  AND Tab1.F_納期 <= :何時まで
GROUP BY Tab1.F_得意先, Tab4.F_得意先略称, Tab1.F_納入品目番号, Tab1.F_在庫品目番号
```

## 見落としやすい仕様（Power BI移植時に効いた点）

| # | 内容 | 効き方 |
|---|---|---|
| 1 | 内示の数量は **`F_数量 + F_調整数`** | 調整数を足し忘れると金額がずれる |
| 2 | 内示の単価結合キーは **`F_在庫品目番号`**（確定は `F_納入品目番号`） | 2つのクエリで結合キーが違う |
| 3 | 内示の単価結合は **INNER JOIN** | 契約単価が無い行は集計から落ちる（確定は LEFT JOIN） |
| 4 | 得意先・納入品目・仕入先・納入先マスタへの **INNER JOIN がフィルタとして効く** | マスタ未登録の伝票は除外される |
| 5 | 確定は **`F_在庫品目番号`** で納入品目マスタに結合 | `F_納入品目番号` ではない |
| 6 | 単価は **有効日で絞っていない**（MAX(開始日) をそのまま採用） | 将来日付の単価も採用される |
| 7 | 内示最新作成日は **納期で絞らず全期間の MAX(F_作成日)** | 得意先ごとの最終受信日 |
| 8 | 確定は出荷完了区分や注残で絞らず **注文数の全量** | 注残金額ではない |

## 移植先

`売上予想.SemanticModel\definition\tables\Fact_受注見込.tmdl` のM式に、
上記2本を `UNION ALL` した1本のSQLとして埋め込んでいます。

Power BIのディメンションとの結合のため、`品目コード` / `在庫品目コード` / `得意先番号` /
`得意先キー` にだけ正規化（制御文字除去→半角化→前後空白除去→大文字化）を追加していますが、
**契約単価を引くための結合キーは元クエリのまま**で、金額の算出には影響しません。
