# 売上予想モデル 突合メモ

作成日: 2026/08/25
対象: `売上予想.pbip`（`売上予想.SemanticModel` / `売上予想.Report`）

## 1. 何を再現したか

内山さん作成の `売上予想8.9.10.11.12.xlsx`（作成者 "Banshuelectric BEI"、最終更新 2026/08/19）は、
月ごとに **予算 / 内示 / 確定** の3値を並べ、`達成率＝(内示＋確定)/予算` を出す1枚もの。
3値はすべて**手入力の直値**で、外部リンクも数式もありませんでした（`内示＋確定` と `達成率` のみ数式）。

このうち **内示と確定の2つは PRONES から再現できる**ことを実データで確認し、モデル化しました。

| 項目 | 再現 | 出所 |
|---|---|---|
| 確定 | できる | `TB_統合注文情報` の `F_注文数 × 契約単価` を `F_納入指定日` の月で集計 |
| 内示 | ほぼできる | `TB_統合計画日別情報` の**得意先ごとの最新 `F_作成日` の行だけ** × 契約単価 を `F_納期` の月で集計 |
| 予算 | できない | PRONES 703テーブルに予算/目標系テーブルは無い → `data\売上予算.xlsx` から取り込む |

契約単価は `TB_納入品目得意先単価情報` から `(納入品目, 得意先)` ごとの最新 `F_開始日 <= SYSDATE` を採用。
同テーブルの `F_開始日` には `#N/A` など日付でない値が混入しているため、
`TRANSLATE(F_開始日,'0123456789','9999999999') = '9999/99/99'` で書式が正しい行だけに絞っています。

## 2. 突合結果

本日（2026/08/25）再計算した値と、Excel（2026/08/19 時点）の比較。

| 月 | 確定(Excel) | 確定(再現) | 差 | 内示(Excel) | 内示(再現) | 差 |
|---|---:|---:|---:|---:|---:|---:|
| 2026/08 | 331,832,554 | 335,387,996 | **+1.1%** | (空欄) | 22,068,135 | 月内の未確定残 |
| 2026/09 | 132,486,642 | 194,072,953 | +46.5% | 240,591,186 | 243,377,177 | **+1.2%** |
| 2026/10 | 128,991,657 | 133,457,088 | **+3.5%** | 274,901,766 | 315,457,591 | +14.8% |
| 2026/11 | 53,038,095 | 68,363,664 | +28.9% | 257,045,913 | 294,959,956 | +14.7% |
| 2026/12 | 6,305,183 | 13,821,754 | +119.2% | 224,409,194 | 221,107,972 | **−1.5%** |

**確定** — 先の月ほど大きく増えているのは 8/19 以降に注文が積み上がった分で、想定どおりの動きです。
8月はすでにほぼ受注済みのため +1.1% に収まっており、**計算式が一致していることの裏付け**になります。

**内示** — 9月 +1.2%、12月 −1.5% と一致。ロジックはほぼ正しいと判断しています。

## 3. 10月・11月に残る約15%の差について（調査済み・原因未特定）

`F_区分` による除外条件で説明できるかを機械的に総当たりで検証しました（区分は
`01 / 03 / 04 / 05 / 06 / 07 / 08 / 14 / 99 / 9A` の10種、4個までの除外組合せを全探索）。

結果、**どの除外組合せも「フィルタなし」より良くなりませんでした**。最良候補（`03,04,05,14` を除外）でも
9月 −3.6% / 10月 +11.1% / 11月 +10.6% / 12月 −5.8% と、一致していた9月・12月を悪化させます。

よって **モデルの既定は「区分フィルタなし」** としています。
残差は 8/19 → 8/25 の間の内示改訂によるものと考えるのが最も自然です
（as-of 8/19 で再構成しても 10月 +13.6% / 11月 +12.0% と同程度の差が残るため、
日付ずれだけでは説明しきれない部分もあります）。

**今後の詰め方** — レポート下部の「計画区分(F_区分)別 内訳」テーブルで区分ごとの金額が見えます。
内山さん／BEI 側に「内示の集計対象（除外している区分や得意先があるか）」を確認し、
判明したらスライサーで区分を外して合わせるか、モデルの既定フィルタに反映してください。

## 4. 当月（8月）の内示の扱い

Excel では8月の内示は空欄ですが、モデルでは約 2,207万円 と出ます。

`TB_統合計画日別情報` は「まだ注文になっていない分」だけを毎回フル送信し直す構造のため、
当月分は月が進むほど小さくなり、月末にはほぼゼロになります。
つまりこの 2,207万円は **8月中にまだ確定注文になっていない見込み残**であり、数値としては正しいものです。
そのまま表示する方針にしていますが、Excel の運用に完全に合わせたい場合は
年月スライサーで当月を外すか、内山さんに扱いを確認してください。

## 5. 実施した検証

1. **埋め込みSQLの直接実行** — TMDL に埋め込んだ4本の `Value.NativeQuery` の SQL を
   そのまま抜き出して `run-query.ps1` で実行し、全件成功を確認。
   `Dim_カレンダー` 62行 / `Dim_品目` 74,648行 / `Dim_得意先` 560行 / `Fact_受注見込` 19,495行。
2. **月別金額の一致** — `Fact_受注見込` を年月×区分種別で集計し、上表の値を得た。
3. **TMDL の構文検証** — Power BI Desktop 同梱の `Microsoft.PowerBI.Tabular.dll` の
   `TmdlSerializer.DeserializeDatabaseFromFolder()` でモデルをパースし、
   7テーブル / 6リレーション / 9メジャー / 1パラメーター がエラーなく読み込めることを確認。
   `Model.Validate()` も `ContainsErrors: False`（既存の `粗利確認` モデルと同じ結果）。
4. **レポート定義の整合** — 全15個のJSONが整形式であること、8個のビジュアルが参照する
   Entity/Property がすべてモデルに実在すること、`definition.pbir` のモデル参照先と
   予算Excelのパスが実在することをスクリプトで確認。

## 5-1. 初回更新でのエラーと対処（2026/08/25 修正済み）

Power BI Desktop での初回更新で以下が出ました。

```
Dim_品目 : テーブル 'Dim_品目' の列 '品目コード' には空白の値が含まれていますが、
           これは、多対一リレーションシップの片側の列やテーブルの主キーとして
           使用される列では許可されていません。(品目コード (54))
Dim_得意先区分 : OLE DB または ODBC エラー: HRESULT からの例外:0x80040E4E
Fact_受注見込  : 前のテーブルの読み込み中にエラーが発生したため、読み込みが取り消されました
```

**原因** — `TB_納入品目情報` に `F_納入品目番号` が空白のみの行が **1件** 存在し、
`TRIM()` の結果が NULL になって主キーに使えませんでした（Oracle では空文字＝NULL）。
`Dim_得意先区分` の `0x80040E4E`（DB_E_CANCELED）と `Fact_受注見込` のエラーは、
これに巻き込まれた**連鎖キャンセル**で、それ自体は不具合ではありません。

**対処** — `Dim_品目` の SQL に `WHERE TRIM(F_納入品目番号) IS NOT NULL` を追加。
念のため `Dim_得意先` にも同じガードを入れました（こちらは該当0件）。

**影響の確認** — 除外したのは 74,648 件中の 1 件のみで、
`TB_統合注文情報` / `TB_統合計画日別情報` の 2026/07 以降に品目番号や得意先番号が空の行は
**両方とも0件**のため、金額は変わりません。

## 5-2. 続いて出た重複キーエラーと、キー正規化の導入（2026/08/25 修正済み）

上記の修正後、今度は次のエラーが出ました。

```
列 '品目コード' (テーブル 'Dim_品目') には重複する値 '13Y063642E//02' が含まれています。
```

**原因** — PRONES のマスタに、Oracle 上は別値だが **Power BI では同一とみなされる**値が混在していました。

1. **品目番号に CR+LF が埋め込まれている行が12件** — Oracle の `TRIM()` は空白しか除去しないため
   `13Y063642E//02` と `13Y063642E//02\r\n` が別キーとして残る。Power BI は末尾の制御文字を無視するため重複扱い。
2. **大文字小文字だけが違う値** — Tabular の文字列比較は**大文字小文字を区別しない**。
   品目で20グループ、得意先でも3グループ（`1257143L`/`1257143l`、`M`/`m`、`W`/`w`）。
3. **全角/半角だけが違う値** — 品目で7グループ（例 `234N2-43211イ`）。

**対処** — キー正規化関数を導入し、`Dim_品目` / `Dim_得意先` / `Fact_受注見込` の
**キー列すべてに同じ正規化を適用**しました。

```sql
-- 制御文字(TAB/LF/CR)除去 → 半角化 → 前後空白除去 → 大文字化
UPPER(TRIM(TO_SINGLE_BYTE(TRANSLATE(<列>, 'A'||CHR(9)||CHR(10)||CHR(13), 'A'))))
```

対象は `品目コード` / `得意先番号` / `得意先キー`。
**契約単価を引くための結合キー（`px` との突合）は従来どおり `TRIM()` のまま**にしてあります。
正規化で別品目の単価が混ざるのを避けるためで、金額の算出ロジックは一切変えていません。

`Dim_品目` には元の表記を確認できるよう `品目コード原文` 列を追加しました。
また `単価` 列は、正規化で複数の品目が1行にまとまった場合でも意味が通るよう
`MAX(契約単価)` から **加重平均（金額÷数量）** に変更しています。

**影響の確認** — 正規化後もキーは完全に一意（品目 74,609 / 得意先 557、重複0）。
Fact 側の品目コードで `Dim_品目` に存在しないものは **0件**。
月別金額は**全月で1円も変わっていません**（下表）。

| | 修正前 | 修正後 |
|---|---:|---:|
| Dim_品目 | 74,648 | 74,609 |
| Dim_得意先 | 560 | 557 |
| Fact_受注見込 | 19,495 | 19,485 |
| 各月の金額 | — | **変化なし** |

> 補足: 検証中に 2026/08 の内示が 22,068,135 → 22,010,271 に動きましたが、これは
> `TB_統合計画日別情報` に当日中の新しいスナップショットが入ったための**データ変動**です。
> 正規化前のクエリを同時刻に再実行しても同じ値になることを確認済みで、修正の影響ではありません。

## 5-3. Power BI サービスへの発行と、ゲートウェイ接続（2026/08/25）

Desktop での更新が通り、サービスへ発行したところ次のメッセージが出ました。

```
ファイルは公開されましたが、切断されました
'売上予想.pbix' が 1 つ以上のデータ ソースに接続されていません。
```

Import モードなのでデータ自体は pbix と一緒にアップロードされており、
**サービス上でレポートを見ることはできます**。できないのは「更新」だけです。
サービス側でデータソースをゲートウェイに紐づける必要があります。

このPCには **オンプレミスデータゲートウェイ 3000.322.3 が導入済み・稼働中**、
**Oracle Client for Microsoft Tools (64bit) 19.27** も入っています。

### 使用するゲートウェイ

`粗利確認` モデルの設定画面から判明した構成:

| 項目 | 値 |
|---|---|
| ワークスペース | `ws-bee-genka-00001` |
| ゲートウェイ | **`bee_tanaka`**（状態: BEEN126 で実行しています） |
| 実行マシン | **BEEN126** = 内山さん／田中さんが作業しているこのPC |
| Oracle データソース名 | **`prones_2`** |

もう1つの `Tanaka.hirohisa_o...` は「正しく構成されていません」なので使いません。

### データソース1: Oracle → 追加作業なし

接続文字列が `粗利確認` と**完全一致**しているため、
`bee_tanaka` に既にある **`prones_2`** をそのままマップするだけです。新規登録は不要。

```
(DESCRIPTION=(ADDRESS=(PROTOCOL=TCP)(HOST=banshu-srv)(PORT=1521))(CONNECT_DATA=(SERVICE_NAME=prones)))
```

### データソース2: 予算Excel（対処済み）

ゲートウェイは `NT SERVICE\PBIEgwService` として動作しており、
`C:\Users\100172\...`（OneDrive を含む）配下は ACL 上読めませんでした。

そのため予算Excelを **`C:\PowerBI\売上予想\売上予算.xlsx`** に移動し、
同フォルダに `NT SERVICE\PBIEgwService` の読み取り権（RX、継承あり）を付与。
パラメーター `予算ファイルパス` も同じパスに更新しました。

- 移動前のファイルは `data\売上予算.xlsx.OLD_使用しません` にリネームしてあります（削除して構いません）
- `C:\PowerBI\売上予想\README.txt` に、この場所に置いている理由を記載しています
- **このフォルダから動かすとサービス側の更新が失敗します**

`bee_tanaka` ゲートウェイは BEEN126（＝このPC）上で動いているため、
このローカルパスはゲートウェイから到達できます。
サービス側で `bee_tanaka` に **ファイル** データソースを1つ追加してください
（完全パス `C:\PowerBI\売上予想\売上予算.xlsx`、認証方法 Windows）。

### 残る作業手順

1. **Desktop** … [パラメーターの管理] で `予算ファイルパス` を `C:\PowerBI\売上予想\売上予算.xlsx` に変更
   → 閉じて適用 → 更新 → 保存 → 発行（上書き）
2. **サービス** … 「接続とゲートウェイの管理」→ `bee_tanaka` → 新規 → **ファイル** データソースを追加
3. **サービス** … セマンティックモデル「売上予想」の設定 → ゲートウェイ接続 → `bee_tanaka` を選択
   → Oracle を `prones_2` に、ファイルを2で作ったものにマップ → 適用
4. **サービス** … 「データソースの資格情報」と「スケジュールされた更新」を設定

### 運用上の依存関係

スケジュール更新は **BEEN126 が起動していてゲートウェイが動いていること**が前提です
（`粗利確認` も同じ前提なので、新たな制約が増えるわけではありません）。
将来このPCを離れる場合は、ゲートウェイの移設と `C:\PowerBI\売上予想\売上予算.xlsx` の配置が必要です。

## 5-4. 元になっていたPRONES個人クエリが判明し、定義を差し替え（2026/08/26）

手作業Excelは、**担当者100061のPRONES個人設定クエリ**の実行結果を貼って作られていることが分かり、
`Fact_受注見込` をその定義どおりに書き換えました。

| クエリ番号 | タイトル | 更新日 |
|---|---|---|
| 230220003 | 確定注文の売上予想 | **2026/08/19**（Excelの最終更新日と一致） |
| 220325008 | 客先別内示情報の売上予想 | 2025/08/25 |
| 161011004 / 161011003 | 得意先最新単価 / 得意先単価最新日 | — |
| 200228004 | 内示最新作成日 | — |

個人クエリは `PRONES.TB_クエリ見出情報` / `TB_クエリ明細情報` に格納されています。
抽出した定義の全文と等価SQLは **[売上予想_元クエリ定義.md](売上予想_元クエリ定義.md)** に残しました。

### 従来の実装との違い（自前の推測 → 正式定義）

| # | 項目 | 差し替え前（推測） | 差し替え後（正式クエリ） |
|---|---|---|---|
| 1 | 内示の数量 | `F_数量` | **`F_数量 + F_調整数`** |
| 2 | 内示の単価結合キー | 納入品目番号 | **在庫品目番号** |
| 3 | 内示の単価結合 | LEFT JOIN | **INNER JOIN**（単価無しは除外） |
| 4 | 各マスタ結合 | なし | **得意先・納入品目・仕入先・納入先へ INNER JOIN**（未登録伝票を除外） |
| 5 | 確定の品目マスタ結合キー | — | **在庫品目番号** |
| 6 | 単価の有効日 | `<= SYSDATE` | **絞りなし**（MAX(開始日) をそのまま採用） |

### 同時刻での差（定義変更だけの影響）

| 月 | 確定(旧) | 確定(新) | 差 | 内示(旧) | 内示(新) | 差 |
|---|---:|---:|---:|---:|---:|---:|
| 2026/08 | 335,343,778 | 336,487,175 | +0.3% | 20,407,337 | 15,510,831 | −24.0% |
| 2026/09 | 204,562,840 | 204,773,309 | +0.1% | 234,708,225 | 218,776,401 | −6.8% |
| 2026/10 | 133,447,827 | 133,631,581 | +0.1% | 315,466,140 | 312,880,679 | −0.8% |
| 2026/11 | 69,705,629 | 69,787,231 | +0.1% | 294,393,318 | 296,046,137 | +0.6% |
| 2026/12 | 13,830,477 | 13,853,851 | +0.2% | 221,978,865 | 224,636,176 | +1.2% |

確定はほぼ変わらず（マスタ未登録の伝票がごく少ないため）。内示は当月・翌月ほど差が出ます。

### 8月19日時点に巻き戻した突合（定義の正しさの検証）

正式クエリを 2026/08/19 時点（`内示最新作成日` と注文の `F_作成日` に上限を付けた状態）で
再現し、Excelの値と比べたもの。

| 月 | 区分 | 再現(8/19時点) | Excel | 差 |
|---|---|---:|---:|---:|
| 2026/08 | 確定 | 332,558,705 | 331,832,554 | **+0.22%** |
| 2026/10 | 確定 | 129,293,372 | 128,991,657 | **+0.23%** |
| 2026/12 | 確定 | 6,419,848 | 6,305,183 | +1.82% |
| 2026/11 | 確定 | 55,683,248 | 53,038,095 | +4.99% |
| 2026/09 | 確定 | 141,267,458 | 132,486,642 | +6.63% |
| 2026/12 | 内示 | 224,566,322 | 224,409,194 | **+0.07%** |
| 2026/09 | 内示 | 273,601,571 | 240,591,186 | +13.72% |
| 2026/10 | 内示 | 312,807,822 | 274,901,766 | +13.79% |
| 2026/11 | 内示 | 288,151,102 | 257,045,913 | +12.10% |

**確定の8月・10月が±0.25%、内示の12月が+0.07%** で一致しており、
移植した定義が正しいことの裏付けになっています。
確定の9月・11月に数%残るのは、`F_作成日` での巻き戻しでは
「既存注文の数量変更」までは戻せないためです。

### 残る論点: 内示の9月・10月・11月が揃って+12〜14%高い

12月が0.07%で一致するのに9〜11月だけ揃ってずれるため、**クエリ定義の問題ではありません**。
基準日を 2026/07/27〜08/19 の24日分すべてで再現しても、9〜11月が一致する日付はありませんでした
（最良でも8/19の+13.8%）。

考えられるのは、Excelの各月のセルが**別々の日に貼られた**、あるいは
**貼った後に手で調整された**といった運用面の要因です。
数値を入力した方（内山さん／100061さん）に、9〜11月の内示をいつ・どの条件で取得したかを
確認していただくのが早いと思われます。

## 6. 未実施（利用者側でお願いしたいこと）

**Power BI Desktop での実更新** — `売上予想.pbip` を開いて「更新」を押し、
- データが取得できること（Oracleクライアント必須）
- 月次サマリーの数値が上表「再現」列と一致すること
- `売上予算.xlsx` を書き換えて更新すると達成率が変わること

を確認してください。上記5の検証でモデル定義とSQLは通ることを確認済みですが、
Oracle への実接続と Power Query の実行だけは Power BI Desktop 上でしか行えません。

## 7. 補足: 使えなかった既存ビュー

同じ目的で作られたとみられる既存ビューが2本ありますが、いずれも**現在エラーで実行できません**。
今回は自前クエリで代替しており、修正はしていません。

- `PRONES.VW_製品注残金額情報` — `TB_納入品目得意先単価情報.F_開始日` に `#N/A` 等が混入しており、
  ビュー内の `F_開始日 <= sysdate`（暗黙の日付変換）が `ORA-01841` で落ちる。
- `PRONES.VW_営業G計画数量` — `TB_機種計画情報` の列構成が変わり
  （`F_得意先番号` / `F_計画年月` / `F_入力日時` が消滅）、定義が追随できず `ORA-04063`。
  なお `TB_機種計画情報` には 2026/07 以降のデータもありません。
